// The results index (U8): createResultsStore implements ResultsStore and ResultsIndex
// (src/playback/types.ts) over the worker's result chunks.

export { createResultsStore, type ResultsStoreOptions } from './store.ts';
